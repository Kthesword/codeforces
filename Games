n=int(input())
left=[]
right=[]
k=0
for i in range(n):
    x,y=list(map(int,input().split()))
    left.append(x)
    right.append(y)
for j in left:
    if j in left:
        for u in right:
            if j==u:
                k+=1
print(k)
